ImageClassification — Web-Based Image Classifier using VLMs

ImageClassification is a simple and clean web application that allows users to upload an image and get a text-based classification using Vision-Language Models (VLMs).
The project includes a backend API for processing images and a frontend interface for interacting with the model in a user-friendly way.
